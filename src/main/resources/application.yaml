spring:
  application:
    name: live-stream

  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: root
    password: password123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

livekit:
  api:
    key: devkey
    secret: secret