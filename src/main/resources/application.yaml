spring:
  application:
    name: live-stream

  datasource:
    url: jdbc:mysql://localhost:3306/lms
    username: root
    password: password123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

livekit:
  api:
    key: devkey
    secret: secret

app:
  jwt:
    secret: MyDefaultSuperSecretKeyThatIsAtLeast256BitsLong!!
    expiration-ms: 86400000